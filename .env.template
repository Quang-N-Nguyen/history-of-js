# Cloudflare Worker + agent shared secret (set in Worker secrets and container env via wrangler).
AGENT_SHARED_SECRET="$(openssl rand -hex 32)"

# Optional: comma-separated browser origins allowed for CORS (Worker). Leave unset for *.
# ALLOWED_ORIGINS="https://your-app.example.com,http://localhost:5173"
